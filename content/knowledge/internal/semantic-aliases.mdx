# Semantic Aliases and Retrieval Associations

This internal document exists to strengthen semantic retrieval when users ask about Alfredo Navas using different wording, alternate phrasing, or indirect concepts.

The following concepts should be treated as semantically connected.

## Professional Work and Client Exposure

clients = companies worked for = brands worked for = organizations worked for = enterprise exposure = corporate work = recognizable companies = professional collaborations = contractor work = agency work = who Alfredo has built for = who Alfredo has developed for

projects = builds = shipped work = delivered products = implementations = platforms built = systems created = portfolio work = case studies = digital products = websites built = things Alfredo has launched

independent clients = freelance work = direct business clients = personal agency work = full ownership projects = Alfredo-led projects

## Technical Skills and Engineering Depth

skills = strengths = technical strengths = strongest areas = specialties = expertise = engineering depth = technical background = what Alfredo is good at = technical mastery = dev stack = technologies mastered

WordPress = Gutenberg = block editor = block themes = plugin development = CMS engineering = custom blocks = WordPress architecture = enterprise WordPress

frontend = React = JavaScript = UI systems = responsive development = component systems = modern CSS = frontend engineering

backend = PHP = Laravel = APIs = database workflows = server logic = full stack capability

AI development = agentic coding = AI workflows = MCP workflows = AI assisted development = AI implementation systems = development with AI = AI accelerated shipping

## Public Speaking and Community Leadership

talks = speaking = presentations = workshops = public sessions = conference talks = livestream teaching = community education = technical education

WordCamp = WordPress events = community conferences = WordPress speaking = open source community participation = meetup involvement

organizer = lead organizer = community leader = meetup founder = WordPress Costa Rica = community builder

public videos = interviews = recorded appearances = YouTube appearances = WordPress.tv = livestreams = Twitch = public media presence

## Product Building and Delivery Scope

what Alfredo builds = project types = systems Alfredo creates = categories of work = digital products built = types of websites = implementation range = shipped platforms

ecommerce = WooCommerce = online stores = product catalogs = selling systems

enterprise sites = corporate websites = large scale CMS = publisher systems = editorial platforms

web apps = PWAs = dashboards = interactive tools = app-like builds = software products

## Professional Identity

frontend developer = full stack developer = WordPress engineer = product builder = technical consultant = senior web developer = implementation specialist = digital architect

teacher = mentor = speaker = organizer = educator = community contributor

designer background = artistic background = visual sensibility = UI intuition = product aesthetics

## Retrieval Intent

Whenever user questions use alternate language around clients, projects, strengths, technologies, conferences, brands, speaking, builds, products, shipped work, or Alfredo's background, the retrieval system should associate those questions with the broader internal knowledge documents, not only with literal keyword matches.